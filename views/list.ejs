<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Danh sách ghi chú</title>
</head>
<body>
  <h1>Danh sách ghi chú</h1>
  <form action="/notes/create" method="POST">
    <input type="text" name="title" placeholder="Nhập tiêu đề ghi chú" required>
    <label>
      <input type="checkbox" name="shared"> Chia sẻ
    </label>
    <button type="submit">Thêm ghi chú</button>
  </form>

  <ul>
    <% notes.forEach(note => { %>
      <li>
        <strong><%= note.title %></strong>
        <% if (note.shared) { %>
          (Đã chia sẻ)
        <% } %>
        <form action="/notes/<%= note.id %>/unshare" method="POST" style="display:inline;">
          <button type="submit">Hủy chia sẻ</button>
        </form>
        <form action="/notes/<%= note.id %>/delete" method="POST" style="display:inline;">
          <button type="submit">Xóa</button>
        </form>
      </li>
    <% }) %>
  </ul>
</body>
</html>
